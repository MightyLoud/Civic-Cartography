# Draft placeholder. The validation workflow regenerates this patch from the
# pinned upstream tree after applying patches 1-3. Replace with the normalized
# Git diff before marking the pull request ready for review.
